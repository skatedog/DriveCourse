$(document).on('turbolinks:load', () => {
  $(".image-slider").slick({
    // autoplay: true,
    // autoplaySpeed: 2000,
    // pauseOnHover: false
  })
  $(".vehicle-slider").slick({
    // autoplay: true,
    // autoplaySpeed: 2000,
    // pauseOnHover: false,
    // arrows: false,
  })
});