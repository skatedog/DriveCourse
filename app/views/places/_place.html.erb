<%= link_to place.name, place_path(place), data: { turbolinks: false } %>
<%= link_to "編集", edit_place_path(place), data: { turbolinks: false } %>
<%= link_to "削除", place_path(place), method: :delete, data: {confirm: "本当に削除しますか？" } %><br>