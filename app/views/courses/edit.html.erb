<div class="window">
  <div class="window__form">
    <%= render "courses/form", course_form: @course_form %>
  </div>
  <div class="window__display">
    <div id="map" class="course-edit__map"></div>
  </div>
</div>

<%= render "courses/map", course: @course_form.course %>